Image1 is missing one double cage: it incorrectly contains17 columns, image2 contains18. Replace ONLY the front port bank of image1 with the FULL 18-column front bank from image2, properly perspective projected, all18 upper/lower paired cages present once =36 sockets. Specifically preserve pairs 11/12,13/14,15/16,17/18 as FOUR distinct columns, never merge13–16 into one. Keep high camera/lid/ears/label/lighting unchanged. Do not just alter text: add the missing physical double cage. Shrink each cage uniformly if needed to fit exactly18, not17 or19.