# Optional official 3D search

Status: no public exact official RH2288 V3 3D/CAD/GLB/glTF/STEP/OBJ/FBX asset or interactive 3D viewer found as of 2026-08-27.

Searched Huawei official product/support pages, documentation package listings, multimedia entries, dynamic rendered resources with Playwright, and public web queries for the exact PID plus `3D`, `CAD`, `STEP`, `GLB`, `glTF`, `OBJ`, `FBX`, `AR`, `Visio`, and `interactive viewer`.

The Huawei user guide itself is account-restricted and a public mirror's full download is protected by reCAPTCHA. No login, reCAPTCHA, paywall, access control, or private API was bypassed. If a public official model later appears, preserve it unchanged here with URL, terms, byte count and SHA-256; it will supplement rather than replace the new build.

Forced-review browser check: Huawei Product Visuals `currentProduct=38` identifies `RH2288 V3 Rack Server`; the public gallery response exposes official raster images and an image ZIP but returns `threeUrl: null`. The page therefore omits the `Visualize in 3D` control that appears for gallery entries with a public 3D viewer. See `huawei-gallery-currentProduct-38.json` and `qa/forced-review-2026-08-24/evidence-inspection/huawei-gallery-rh2288v3.png`.

Rotation-review browser recheck (2026-08-27): the public entry remains accessible, still has only `Download All`, and its live JSON again returns `threeUrl: null`. Current real-browser evidence is retained in `qa/rotation-review-20260827/after/research/`.
