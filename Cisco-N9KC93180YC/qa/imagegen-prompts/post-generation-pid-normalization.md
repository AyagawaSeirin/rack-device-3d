# Post-generation fan PID normalization

The dedicated rear image-generation call was executed against the preserved legacy FX hardware-guide nomenclature `NXA-FAN-30CFM-B`. The user's final exact configuration lock subsequently normalized the installed ordering PID to `NXA-FAN-30CFM-PI`.

This does not change the generated visual invariant: four installed port-side-intake fan trays with burgundy latches, positionally matched to two burgundy `NXA-PAC-500W-PI` AC PSUs. The original rear prompt remains unchanged as an accurate execution ledger; final identity, source lock, feature inventory, GLB metadata, and report use `NXA-FAN-30CFM-PI`.
