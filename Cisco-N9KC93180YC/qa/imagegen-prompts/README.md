# Image-generation gate

No built-in image-generation call was made.

The assembly identity is `BLOCKED` before face production: the user screenshot does not uniquely resolve `N9K-C93180YC-EX` versus `N9K-C93180YC-FX` or the blue/burgundy AC airflow module set. The 3D skill explicitly requires stopping before image generation when an identity-bearing non-bottom fact is unresolved.

`source/face-source-lock.csv` therefore records all faces as `BLOCKED_IDENTITY_UNRESOLVED` or `NOT_REACHED_IDENTITY_GATE`; no prompt can truthfully lock an exact PID/configuration yet.

After the user provides the exact chassis PID and installed AC PSU/fan PIDs, six independent built-in calls are required, one per face, with real exact-device sources as the binding identity and photographic-style references.
