# Dell PowerEdge R730 2.5-inch / 16-SFF exact exterior asset

This directory is intentionally isolated to the user screenshot's Dell PowerEdge R730 2.5-inch physical variant. It contains the locked source lineage, six generated canonical faces, independently modeled visible exterior, standard/web GLBs, two independent browser-viewer packages and QA evidence.

Coordinate convention:

- `+X`: device right as seen from the front
- `+Y`: up
- `+Z`: front
- millimetres are converted to glTF metres on export

Configuration exclusions: R730xd, R720/R720xd, 3.5-inch/LFF front, 8-SFF front, front bezel, rear flex drives, fitted add-in cards, DC or single PSU.

Because no exact underside photograph was found after the documented search, the final result is expected to close as `PASS_WITH_BOTTOM_FALLBACK`.

Final release status: **PASS_WITH_BOTTOM_FALLBACK** (18/18 gates). See `qa/final-report.md` and `qa/audit.json` for GLB hashes, actual-load evidence, official-3D discovery and residual risks.
